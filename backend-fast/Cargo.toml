[package]
name = "backend-fast"
version = "0.1.0"
edition = "2021"

[dependencies]
# tokio = { version = "1.0", features = ["full"] }
# axum = "0.7"
# sqlx = { version = "0.7", features = ["postgres", "runtime-tokio-rustls"] }
# tungstenite = "0.21"
