const lines = [
  "# Runner Help",
  "",
  "Primary chain:",
  "0. npm run runner:readiness:compact",
  "   Optional. Checks local readiness and safety guards without API calls.",
  "1. npm run runner:goal -- --roles <roles> \"request\"",
  "   Safe rehearsal. Applies, verifies, rolls back, then reports.",
  "2. npm run runner:quick",
  "   Read-only. Shows compact status and compact continue options for the latest live run.",
  "   Use runner:quick:mock for mock runs or runner:quick:any for the newest run regardless of mode.",
  "3. npm run runner:accept:preview",
  "   Preview keeping the latest proposed edits.",
  "4. npm run runner:accept",
  "   Keep the latest proposed edits intentionally.",
  "5. npm run runner:latest:status",
  "   Optional detailed status view. Use runner:latest:status:compact for one-screen output.",
  "",
  "When you already have a run id:",
  "- npm run runner:status -- <run-id>",
  "- npm run runner:status:compact -- <run-id>",
  "- npm run runner:continue:compact -- <run-id>",
  "- npm run runner:continue:a -- <run-id>",
  "- npm run runner:continue:b -- <run-id>",
  "- npm run runner:continue:b:execute -- <run-id>",
  "- npm run runner:continue:c -- <run-id>",
  "",
  "Low-cost checks:",
  "- npm run runner:readiness:compact",
  "- npm run runner:preflight:compact",
  "- npm run runner:argument-guard:smoke",
  "- npm run runner:accept-guard:smoke",
  "- npm run runner:accept-unlock:smoke",
  "- npm run runner:goal:budget -- --roles frontend \"request\"",
  "- npm run runner:plan:budget -- --roles frontend \"request\"",
  "- npm run runner:goal:mock -- --roles frontend,java \"request\"",
  "- npm run runner:plan:mock -- --roles frontend,java \"request\"",
  "- npm run runner:quality:smoke",
  "- npm run runner:workflow:quality-smoke",
  "- npm run ci:dry-run",
  "",
  "Reports:",
  "- npm run runner:reports",
  "   Build a report index for all runs.",
  "- npm run runner:reports:live",
  "   Build a report index for live runs only.",
  "- npm run runner:reports:mock",
  "   Build a report index for mock runs only.",
  "",
  "Safety notes:",
  "- runner:goal is a rollback rehearsal; it does not keep edits.",
  "- runner:quick is read-only; it only helps you choose the next command.",
  "- runner:quality runs a local post-apply quality gate for changed files.",
  "- runner:workflow:quality-smoke confirms workflow blocks bad applied edits and rolls them back.",
  "- runner:argument-guard:smoke confirms missing npm `--` style argument loss is blocked before API calls.",
  "- runner:goal:budget stops when estimated API cost exceeds $0.10.",
  "- runner:plan:budget stops when estimated API cost exceeds $0.05.",
  "- runner:accept keeps edits. Use it only after reviewing the report.",
  "- runner:accept is locked until the latest run has passed a rollback rehearsal and quality gate.",
  "- runner:accept-unlock:smoke confirms a successful rehearsal unlocks Option B without keeping files.",
  "- runner:accept is an alias for the latest live run's Option B execute path.",
  "- Option C is preview-only because it means reject/replan.",
];

console.log(lines.join("\n"));
