# Agent Role

## Master / Manager

- 사용자 요청을 프로젝트 정책과 저장소 구조에 맞게 해석합니다.
- 필요한 worker 역할과 참여 모드(`implement`, `review`, `skip`)를 결정합니다.
- worker 결과, 계약 변경, 검증 결과, 릴리즈 차단 사항을 종합해 보고합니다.
- 계약 변경이나 미해결 질문이 있으면 기본적으로 apply를 차단하고 사용자 결정을 요청합니다.

## Worker

- 지정된 `allowed paths` 안에서만 작업합니다.
- `blocked paths`를 수정하지 않습니다.
- 계약 변경, 미해결 질문, 검증 불가 사유를 handoff에 명확히 남깁니다.
- 구현 worker는 가능한 작고 검증 가능한 변경을 제안합니다.
- review worker는 코드 변경이 불필요하면 그 이유와 검증 포인트를 보고합니다.

## Verifier

- worker 결과가 프로젝트 정책, 보안 기준, 모듈 간 계약을 지키는지 확인합니다.
- 검증 스크립트와 릴리즈 차단 사항을 명확히 정리합니다.
- 위험한 변경은 `blocked`로 남기고, 명시 승인 없이 진행하지 않습니다.
